import SealCityselect from './src/main';

/* istanbul ignore next */
SealCityselect.install = function(Vue) {
    Vue.component(SealCityselect.name, SealCityselect);
};

export default SealCityselect;
